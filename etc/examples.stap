{ quote
   str! "'" "'"
   "021" 3 shake
   str str }

{ @one
  quote "$echo -ne " str
  query drop }

{ @all
  count vec
  "\n" pop
  { &mapcat
    vec! split
    str! "\n" swap str push str pop }
  reduce
  push quote "$echo -ne " str
  query drop drop }

